{
  "name": "vale-server",
  "version": "1.0.10",
  "description": "API backend for Vale with Vale at Red Hat rules",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gaurav Nelson",
  "license": "Apache-2.0",
  "dependencies": {
    "@fastify/rate-limit": "^10.3.0",
    "fastify": "^5.5.0"
  }
}
